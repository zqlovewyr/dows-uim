spring:
  application:
    name: cop-sms-admin
  cloud:
    consul:
      host: consul-sit.mid.io
      port: 8500
      config:
        enabled: ${SPRING_CONFIG_ENABLED:false}
        profile-separator: "-"
        prefixs:
          - config
        defaultContext: ${spring.application.name}
        format: YAML


