server:
  port: 9011
  undertow:
    buffer-size: 1024
    direct-buffers: true
    threads:
      io: 4
      worker: 32
#  servlet:
#    context-path: /${spring.application.name}


spring:
  application:
    name: dows-uim-user
  profiles:
    include: mysql,mybatis,mvc,redis,alipay
    active: sit

# 微信配置
wx:
  miniapp:
    configs:
      - appid: wxf25ba88aa880e9cf #微信小程序的appid
        secret: e9ad6cbaa8961e0caaeda4602156c979 #微信小程序的Secret
        token: #微信小程序消息服务器配置的token
        aesKey: #微信小程序消息服务器配置的EncodingAESKey
        msgDataFormat: JSON
#debug: true
