server:
  port: 9000
  undertow:
    buffer-size: 1024
    direct-buffers: true
    threads:
      io: 4
      worker: 32
#  servlet:
#    context-path: /${spring.application.name}


spring:
  application:
    name: cop-sms-admin
  profiles:
    include: mysql,mybatis,mvc,rocketmq,redis,msg,es,https,jackson,quartz,consul
    active: ${profile:sit}
#debug: true
