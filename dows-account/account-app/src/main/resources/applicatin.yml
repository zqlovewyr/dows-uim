server:
  port: 8080

spring:
  application:
    name: account
  profiles:
    include: classpath*:/account/application-mysql.yml,classpath*:/account/application-mybatis.yml,classpath*:/account/application-mvc.yml
    active: dev

  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: true